[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
# Check https://flit.readthedocs.io/en/latest/pyproject_toml.html for all available sections
name = "ansys-cfx-api"
version = "0.1.dev0"
description = "A Python wrapper for the Ansys CFX API."
readme = "README.rst"
requires-python = ">=3.9,<4"
license = {file = "LICENSE"}
authors = [
    {name = "ANSYS, Inc.", email = "pyansys.support@ansys.com"},
]
maintainers = [
    {name = "PyAnsys developers", email = "pyansys.core@ansys.com"},
]

classifiers = [
    "Development Status :: 4 - Beta",
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]
dependencies = [
    "importlib-metadata >=4.0",
]

[project.optional-dependencies]
tests = [
    "pytest==8.1.1",
    "pytest-cov==5.0.0",
]
doc = [
    "ansys-sphinx-theme==0.14.1",
    "numpydoc==1.7.0",
    "sphinx==7.2.6",
    "sphinx-copybutton==0.5.2",
]


[tool.flit.module]
name = "ansys.cfx.api"

[project.urls]
Source = "https://github.com/ansys-internal/ansys-api-cfx"
Tracker = "https://github.com/ansys-internal/ansys-api-cfx/issues"
Homepage = "https://github.com/ansys-internal/ansys-api-cfx"


[tool.black]
line-length = 100

[tool.isort]
profile = "black"
force_sort_within_sections = true
line_length = 100
default_section = "THIRDPARTY"
src_paths = ["doc", "src", "tests"]

[tool.coverage.run]
source = ["ansys/cfx"]

[tool.coverage.report]
show_missing = true

[tool.pydocstyle]
add-ignore = ["D400"]
